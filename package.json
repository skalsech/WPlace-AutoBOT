{
  "name": "wplace-auto-bot",
  "version": "0.0.1",
  "type": "module",
  "keywords": [
    "tampermonkey",
    "userscript",
    "wplace",
    "automation"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "prepare": "npm run generate-languages",
    "prebuild": "npm run lint:js:fix && eslint src/ --quiet",
    "generate-languages": "node scripts/generate-languages.js",
    "build": "npm run generate-languages && node build/build.mjs",
    "build:prod": "npm run generate-languages && cross-env NODE_ENV=production node build/build.mjs",
    "dev:build": "npm run generate-languages && cross-env NODE_ENV=development node build/build.mjs",
    "dev:watch": "chokidar \"src/**/*\" -c \"npm run dev:build\"",
    "serve": "serve --config serve.json --listen 8000",
    "dev": "npm-run-all dev:build --parallel serve dev:watch",
    "lint": "npm run lint:js && npm run lint:css",
    "lint:js": "eslint src/ --quiet",
    "lint:js:fix": "eslint src/ --fix",
    "lint:css": "stylelint \"src/assets/css/**/*.css\"",
    "lint:css:fix": "stylelint \"src/assets/css/**/*.css\" --fix",
    "format": "prettier --write src/ build/",
    "format:check": "prettier --check src/ build/",
    "clean": "rimraf dist",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest run --coverage",
    "coverage:clean": "rimraf coverage node_modules/.vite",
    "preview": "npm run build:prod && serve --listen 8000"
  },
  "devDependencies": {
    "@eslint/js": "^9.34.0",
    "@vitest/browser": "^3.2.4",
    "@vitest/coverage-v8": "^3.2.4",
    "autoprefixer": "^10.4.21",
    "chokidar-cli": "^3.0.0",
    "cross-env": "^10.0.0",
    "esbuild": "^0.25.9",
    "esbuild-plugin-postcss": "^0.3.0",
    "eslint": "^9.34.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.4",
    "glob": "^11.0.3",
    "globals": "^16.3.0",
    "happy-dom": "^20.0.2",
    "lodash": "^4.17.21",
    "npm-run-all": "^4.1.5",
    "postcss": "^8.5.6",
    "postcss-import": "^16.1.1",
    "postcss-nesting": "^13.0.2",
    "prettier": "^3.6.2",
    "rimraf": "^6.0.1",
    "serve": "^14.2.5",
    "stylelint": "^16.23.1",
    "stylelint-config-standard": "^39.0.0",
    "tsc": "^2.0.4",
    "vitest": "^3.2.4"
  }
}
